pipeline {
    agent { label 'GOL'}
    triggers {
        cron ('H * * * *')
        pollscm ('* * * * *')
    }
    stages {
	stage('scm') {
	     steps {
                  git branch: 'master', url: 'https://github.com/bhupesh019/game-of-life.git'

	     }
	}
	stage('build') {
	     steps {
                 sh 'mvn package'

	      }
	}
    }
    post {
        success {
	    archive '**/*.war'
            junit '**/TEST-*.xml'
	 }
    }
}