node('GOL') {
    stage('scm') {
        git 'https://github.com/bhupesh019/game-of-life.git'
    }
    Stage('build') {
        sh 'mvn clean package'
    }
    stage('postbuild') {
        junit '**/TEST-*.xml'
        archive '**/*.war'
    }
}